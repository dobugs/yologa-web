import { CSSObject } from '@emotion/react';

const login: CSSObject = {
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'center',
  alignItems: 'center',
  gap: 8,
  padding: 24,
  height: 'calc(var(--vh, 1vh) * 100)',
};

export { login };
