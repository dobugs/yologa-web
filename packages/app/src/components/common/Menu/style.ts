import { CSSObject } from '@emotion/react';

const wrap: CSSObject = {
  padding: '0 2.4rem',
};

const link: CSSObject = {
  display: 'flex',
  gap: 16,
  alignItems: 'center',
};

const iconWrap: CSSObject = {
  flex: '0 0 3.6rem',
};

const name: CSSObject = {
  fontSize: 18,
  fontWeight: 'normal',
};

export { wrap, link, iconWrap, name };
